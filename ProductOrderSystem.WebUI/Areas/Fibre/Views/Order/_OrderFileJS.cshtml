@using ProductOrderSystem.Domain.Fibre.Models

<script type="text/javascript">
    var files = [];
    @{
        List<OrderFile_Fibre> l = ViewBag.OrderFiles as List<OrderFile_Fibre>;
        if (l != null)
        {
            foreach (OrderFile_Fibre k in l)
            {
                @:files.push({ filename: '@k.FileName', url: '@k.GoogleFileUrl', fileID: '@k.ID' });
            }
        }
    }
</script>